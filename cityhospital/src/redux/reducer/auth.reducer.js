 import * as ActionTypes from "../ActionTypes"
 
const initval = {
    isload: false,
    user: null,
    error: ''
}
export const authreducer = (state = initval, action) => {
    switch (action.type) {
        default:
            return state;
    }
} 